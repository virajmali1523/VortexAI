{
  "name": "ai-chatbot-monorepo",
  "version": "1.0.0",
  "description": "AI Chatbot Workspace running Express backend and Ionic Angular frontend",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && ionic serve",
    "start": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
